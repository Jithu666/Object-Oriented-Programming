#include<stdio.h>
int main(){
    int arr[] = {3, 7, 1, 9, 5};
    int evenCount = 0;
    int oddCount = 0;

    for(int i = 0; i < n; i++){
        if(i % 2 == 0)
        evenCount++;
        else
        oddCount++;
    printf("the even count is : %d and the odd count is %d\n", evencount, oddCount);
    }
return 0;
}